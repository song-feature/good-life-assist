# 需求描述
我想增加 channel的概念，channel 是与 agent交互的渠道，目前可能有以下两类

- web(默认): 最全能的功能，agent交互后不仅能够回复文字，也可以在功能区展示对应的页面
- im: im 通道用于一些 im交互场景，用户可以通过 im软件与我们的 agent交互，此时仅通过 im通道
回复消息给用户，不需要展示页面 功能区等信息


请你帮我基于现在的架构 增加通道的概念，并实现一个 im 通道，有以下要求

1. 你需要抽象 im 通道的配置页面，可以是不同的 im 实现不同的配置页面，但是需要可以在 admin页面统一管理起来
同时 admin 页面要具备测试能力，比如配置了必要的参数(如 app_id，ak等) 要能够校验是否正确或者能够让用户发起测试
2. im 通道可以启用和关闭，你需要保证 im通道启用后，实时保活，保证 im通道是畅通的
3. 我们没有域名，不会暴露端点出去，所以 im 通道一定是基于 长链接、事件驱动等等的形式


# im 飞书接入需求

请你帮我接入飞书机器人，接入文档如下
https://open.feishu.cn/document/server-side-sdk/python--sdk/preparations-before-development

